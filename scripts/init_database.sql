drop database if exists DataWarehouse; Drops the database
create Database DataWarehouse; Creates
use DataWarehouse; uses a Database
  
  -- create schema - it's a container/folder to organise the data 
 create schema bronze;
 create schema silver;
 create schema gold;
